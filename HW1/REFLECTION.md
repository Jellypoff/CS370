Claude code is a very powerful tool that drastically increases workflow and efficiency. I think that the agent was most reliable in following the CLAUDE.md constraints, with actual coding, and it was very good at explaining concepts or code that I did not understand or wanted more clarification on. I think that the agent was not very reliable with overstepping. For exmaple, I wanted to roughly follow the milestones and the healthy history described in the companion guide. Claude wanted to merge multiple steps as it concluded that it was the most efficient thing to do. But I wanted to follow a somewhat straight and structured slices. Claude also asked me many times if I wanted to make skeleton mains or tests or methods. This was my fault because I didn't specify to make these skeletons a certain way, or at all. One bug that Claude wanted to introduce that I caught was with rb_validate and how it needed to maintain parent pointers in episode 2. I had to know how Claude was maintaining pointers very well because my deletion implementation relied on the sentinel having a meaningful parent during fixup because if this wasn't done right, deletion fixup could walk throuh the wrong parent. It was also really hard to get my environment set up because I thought I read in the spec that we had to do everything locally. The spec doesn't say this and the tests also didn't need to run locally. I asked Claude for help on debugging this and it made a lot of mistakes with that. Evenetually I used wsl to run make test, make asan, and make memcheck.
The thing that C most suprised me the most was memory management. This is an obvious one and I've coded in C++ before but never C. Memory management is similar in C and C++ but things like malloc() and free() are very important for memory management. There is no constructor that automatically runs or for bugs like use-after-free, I explicitly had to handle freeing node-> key and then the node and how after freeing the node I can't do node -> parent but I have to manage all of that. There is also the difference between NULL and nil. They are not the same because NULL essentially means nothing(this isn't always true but generlly its true), but nil is an actual struct in memory.